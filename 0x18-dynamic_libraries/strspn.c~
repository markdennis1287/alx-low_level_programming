#include "main.h"
/**
 * _strspn - Computes the length of the initial segment of s1
 * consisting of only the characters in s2
 * @s1: The string to be searched
 * @s2: The string containing the characters to match
 *
 * Return: The length of the initial segment of @s1 containing only the
 * characters present in @s2
 */
size_t _strspn(const char *s1, const char *s2)
{
/* My implementation */
return (0);
}
