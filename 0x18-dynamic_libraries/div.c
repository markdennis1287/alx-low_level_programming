#include "main.h"
/**
 * div - Divides two integers
 * @a: The dividend
 * @b: The divisor
 *
 * Return: The quotient of the division, or 0 if divisor is 0
 */
int div(int a, int b)
{
if (b != 0)
return (a / b);
else
return (0);
}
